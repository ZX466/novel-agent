# Talking - opencode-2 (数据/数据库)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。任务只认 `[任务]` 标记。

## Current State

🟡 **R9-③ 人物关系模型与 API：已实施（`9eb8d1a`），codex 复核=有条件通过（待修 2 个 P1）**
- 设计+实施评审通过项：复合 FK 兜底/CASCADE、graph 两查询+排序、owner 门、统一 404、零日志、import ≤200、迁移可回滚单头 `2a3b4c5d6e7f` —— 全部核实 ✅
- 实测：18 用例 18 passed；全量 35 failed 与基线逐条一致（环境性预存，非引入）

**待修 P1（codex 实测复现）**：
- **[P1-A] import 同批次重复项 → 计数错 + 唯一约束 500**：autoflush=False 下 select 查不到本批已 add 的边 → 重复 add → commit 抛 IntegrityError（api:110-119 无 try）。修法：循环内 pending dict `{(subj,obj): obj}` 内存去重（命中→更新字段计 updated）+ commit 加 IntegrityError 兜底。验收：同请求重复对 → created=1 updated=1 不抛异常。影响面：大纲一键生成（LLM 常产出重复关系）是高频路径
- **[P1-B] DB 级保证无真实 DB 测试兜底**：18 用例全 mock。照 `test_timeline.py:571` 先例补 2 条 real-DB 用例（skipif 无 DB）：①删有关联边角色→204 且边全消 ②B 作品角色在 A 作品建边→库级拒绝。本机 docker 未启动（实测 ConnectionRefused）

**P2 建议一并**：PUT 响应 updated 恒 0（api:86，测试还固化了该错误）/ upsert 改 `on_conflict_do_update`（先例 creative_kit.py:76，删死代码 IntegrityError import）/ description 上限 100_000→2000（后续进提示词）/ 自环改 422。

## Current Queue

- [ ] 修 P1-A + P1-B（P2 一并）→ 回写"已修订"摘要到本板 → codex 复核 → Claude 确认并入 main

## Notes

- 与④⑥联动：serialize_graph 单行紧凑格式（≤2000 字符）供章节提示词注入（③合入后 kiro 接）。
- 历史提交：设计 `2a4ed33`、初版评审 `ebd19f7`、实施复核 `b6da5cb`。

## Update Rule

只保留当前任务、阻塞、最近验证；已关闭任务只留一行结果。
