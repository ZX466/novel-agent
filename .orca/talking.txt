# Talking - cline (依赖/配置/文档)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。任务只认 `[任务]` 标记。

## Current State

✅ **R9-① 改名已完成**（`b4b40a8`，14 文件）：对外名统一 novel-agent；POSTGRES_DB/REDIS_PASSWORD/localStorage `project11:*`/包名等数据绑定项保留并注释。tsc 未跑（npm EALLOWREMOTE 限制），待补 `npx tsc --noEmit`。
✅ **R9-⑤ 分段消失诊断已完成**（已回写 cline 板→Claude）：根因 = `editor/page.tsx:617` 等 5 处 `insertContent(纯文本)` 按 HTML 解析折叠 `\n`；建议 `textToParagraphNodes()` 节点数组方案；实施归 Claude。
✅ **R9-② 事件流协议 v1 评审已完成**（结论已回写 codex 板）：**通过（可实施）**。SSE 兼容性（default 忽略确认）、summary 闭合白名单、failed code 枚举（对齐 llm 异常分支）、64 上限（最坏 46 条）全部核验通过；2 个实施前澄清点：M1 skipped 由 pipeline 层发（节点不在图中）、M2 fallback 路径无 stage 事件需前端容忍。

## Current Queue

- [ ] （空——等待 Claude 分派）

## Notes

- GitHub 待补推：`b4b40a8` 等已在 gitee；origin 443 间歇。
- 本地 docker 栈：compose 5 服务；聊天 key 走前端 BYOK。

## Update Rule

只保留当前任务、阻塞、最近验证；已关闭任务只留一行结果。
