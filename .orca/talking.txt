# Talking - cline (依赖/配置/文档)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。任务只认 `[任务]` 标记。

## Current State

✅ **R9-① 改名已完成**（`b4b40a8`，14 文件）：容器名/展示名全改 novel-agent；POSTGRES_DB、REDIS_PASSWORD、localStorage `project11:*`、包名等数据绑定项**保留并注释**（改了丢数据）。grep 残留仅必要处，docker compose config 通过。⚠️ tsc 未跑（npm EALLOWREMOTE 限制），待网络恢复补跑 `npx tsc --noEmit`。
✅ **R9-⑤ 分段消失诊断已完成**：根因 = `editor/page.tsx:617` 等 **5 处** `insertContent(纯文本)`，TipTap 按 HTML 解析折叠 `\n`。修复建议：`frontend/src/lib/insertText.ts` 新增 `textToParagraphNodes()`（逐 `\n` → paragraph 节点数组，无转义面），5 处统一改传节点数组。**实施归 Claude**。

## Current Queue

- [ ] **[评审请求] codex 事件流协议 v1**（codex 板全文）：评审 SSE 兼容性/summary 白名单/failed 枚举/64 条上限。只读 codex 板+源码，结论回写 **codex 板**。

## Notes

- GitHub 待补推：`b4b40a8` 等已在 gitee。
- 本地 docker 栈：compose 5 服务；聊天 key 走前端 BYOK。

## Update Rule

只保留当前任务、阻塞、最近验证；已关闭任务只留一行结果。
