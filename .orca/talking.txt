# Talking - opencode-2 (数据/数据库)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。任务只认 `[任务]` 标记。

## Current State

✅ **R9-③ 人物关系模型与 API：设计已交，双评审结果就绪**（完整设计在 git 历史 `2a4ed33`/板历史）：
- Claude 终审：**通过**（5 约束：graph 走 owner 门/import 部分成功/PUT 幂等/联动④⑥/迁移前复查 head）
- Codex 评审：**有条件通过**——P1 三项必须修订后再实施（见下），P2 建议一并处理

## P1 修订任务（Codex 要求，先改设计再实施）

- **[P1-1] FK 级联**：subject_id/object_id 的 FK 全部 `ondelete="CASCADE"` + ORM `passive_deletes=True`（否则删角色 500）。验收：删有关联边的角色→204 且边全消
- **[P1-2] 跨作品 DB 兜底**（照 plot_events 先例 `c0d1e2f3a4b50`）：characters 加 `UniqueConstraint(novel_id, id)`；两个复合 FK `(novel_id, subject_id)→characters(novel_id, id)`、`(novel_id, object_id)→…`。验收：B 作品角色 id 在 A 作品建边→409/404 且无边
- **[P1-3] GET /graph 两查询+排序**：select 角色（where novel_id）+ select 边（where novel_id），内存映射；nodes 按 name 升序、edges 按 (subject_id, object_id) 稳定排序；响应不含 novel_id

P2（实施时一并）：无向唯一键语义待定（先确认拖拽语义）/strength CheckConstraint 0-10 + Pydantic ge=0 le=10 + relation_type strip+长度限制/import ≤200 条+幂等+created/updated/skipped 计数/updated_at onupdate。

## Current Queue

- [ ] **回写 P1 修订摘要到本板** → codex 复核 → Claude 确认后实施迁移+代码

## Notes

- Codex 评审全文见 git 历史 `ebd19f7`（含 S-a~S-c 安全补充：owner 门/404 统一防探测/日志不记 description）。
- 与④⑥联动：serialize_graph 单行紧凑格式（≤2000 字符）供提示词注入。

## Update Rule

只保留当前任务、阻塞、最近验证；已关闭任务只留一行结果。
