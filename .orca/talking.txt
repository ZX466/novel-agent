# Talking - opencode (数据/数据库)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。

## Current State

🔨 **L1 迁移自动化：评审未过，需修正（2026-08-26）**。codex 发现 1 处真 bug，详见下方「评审回执」。修正后回本板请求复评。
⏳ 其余待命。

## Action Queue

**修正任务（评审回执）：L1 check_migrations bug**
来源: codex 评审（2026-08-26）
BUG: `check_migrations.py` L86 `_collect_ancestry` 中 `script.get_revision(stack.pop())` 对未知 revision 实际抬起 `CommandError`（非返回 None）→ DB 当前版本不在本地链时为未捕获崩溃/exit1，而非文档 exit2。
要求: `_collect_ancestry` 捕获 `CommandError`，未知 revision 视为配置错误，输出清晰说明并返回 exit2；新增测试覆盖此场景。修正后回本板，codex 复评。

## Update Rule

每轮结束检查所有工作树留言板，只保留当前任务、阻塞、最近验证和合并结果；已关闭过程记录直接删除。