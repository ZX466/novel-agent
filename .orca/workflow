# Orca Workflow — 多 Agent 协作规范

> **每个 Agent 行动前必须先读取本文档，找到自己负责的能力域，严格遵守边界，不要越界。**

---

## 一、核心原则

1. **每个能力域一个推荐 Agent**，不重复分配；评审 Agent 负责交叉验证。
2. **Agent 之间不直接对话**，有协作需求时通过各自工作树的 `.orca/talking.txt` 间接留言。
3. **行动前读取本文档**，确认自己的职责范围；遇到超出范围的任务，标记并转交。
4. **每次完成工作后**，检查当前工作树状态，在 `talking.txt` 中留下进展记录。
5. **最终合并到 main 分支**，推送到 Gitee 和 GitHub 双远程仓库。

---

## 二、Agent 注册表

完整注册表见 `.orca/agent-registry.md`。以下是速查表：

| 能力域 | 推荐 Agent | 评审 Agent |
|--------|-----------|-----------|
| 架构 / 代码质量 / 逻辑 / 测试 | Claude | Codex |
| 依赖 / 配置 / 文档 | cline | Claude |
| 安全 / 合规 / 风险 | Codex | cline |
| 性能 | Pi | cline |
| 数据 / 数据库 | opencode | Codex |
| 接口 / 兼容性 | kilo | Claude |
| 前端 / 体验 / 发布 / 运维 | Claude | Codex |

**未覆盖的能力域保持空白**，由用户决定后续补充。

---

## 三、任务分发流程

```
用户需求 → Claude（主协调）
  │
  ├─ 分析需求 → 确定涉及哪些能力域
  ├─ 按能力域分配给对应 Agent（通过 talking.txt 留言）
  └─ 等待各 Agent 完成 → 评审 Agent 复核 → 合并到 main
```

### 任务留言格式（写入对方工作树的 talking.txt）

```
[任务] <简短标题>
来源: <你的 Agent 名称>
时间: <YYYY-MM-DD HH:MM>
优先级: P0/P1/P2
描述:
  <具体任务描述>
  <涉及文件或模块>
  <期望产出>
  <截止时间（可选）>
---
```

### 回复格式

```
[回复] <对应任务标题>
状态: 已接受 / 进行中 / 已完成 / 需澄清 / 超出范围
进展:
  <当前进度或结果>
疑问:
  <如有疑问，列出>
---
```

---

## 四、各 Agent 职责边界

### Claude（主协调 + 架构 + 前端 + 代码质量）
- **负责**：整体架构设计、代码质量把控、逻辑正确性、前端开发与体验、发布与运维、测试策略
- **评审**：cline 负责的依赖/配置/文档 工作；kilo 负责的接口/兼容性 工作
- **不做**：性能深度优化（找 Pi）、安全审计（找 Codex）、数据库设计（找 opencode）

### cline（依赖 + 配置 + 文档）
- **负责**：依赖管理与升级、构建配置、环境配置、项目文档撰写与维护
- **评审**：Codex 负责的安全/合规/风险 工作；Pi 负责的性能 工作
- **不做**：深度架构设计（找 Claude）、代码重构（找 Claude）

### Codex（安全 + 合规 + 风险）
- **负责**：安全审计、合规性检查、风险评估、漏洞修复
- **评审**：Claude 负责的架构/代码质量/逻辑/测试 工作；opencode 负责的数据/数据库 工作
- **不做**：功能开发（找 Claude）、依赖升级（找 cline）

### Pi（性能）
- **负责**：性能分析、瓶颈定位、性能优化、基准测试
- **评审**：无（由 cline 评审 Pi 的工作）
- **不做**：功能开发、安全审计

### opencode（数据 + 数据库）
- **负责**：数据库设计、数据模型、查询优化、迁移脚本、数据服务
- **评审**：无（由 Codex 评审 opencode 的工作）
- **不做**：前端开发、架构决策（需与 Claude 协作）

### kilo（接口 + 兼容性）
- **负责**：API 设计、接口规范、向后兼容、版本化、集成测试
- **评审**：无（由 Claude 评审 kilo 的工作）
- **不做**：前端 UI、数据库内部优化

---

## 五、代码与提交规范

### Git 身份
- 提交姓名: `ZX666X`
- 邮箱: `zx19836980213@outlook.com`
- 双远程: Gitee (ZX666X) + GitHub (ZX466)

### 提交规范
Conventional Commits：`feat:` / `fix:` / `refactor:` / `docs:` / `test:` / `chore:` / `perf:` / `ci:`

### Python 工作流
- 使用 `uv` 构建虚拟环境：`uv venv .venv --python 3.11`
- 在虚拟环境中进行开发和测试
- 后端位于 `backend/` 目录，`uv pip install -r requirements.txt`

---

## 六、工作完成检查清单

每次完成一项工作后，逐项检查：

- [ ] 代码符合项目既有风格和规范
- [ ] 相关测试通过（新增功能必须带测试）
- [ ] 文档已同步更新（如涉及）
- [ ] 无敏感信息泄露（API Key、密码等）
- [ ] 已在 `.orca/talking.txt` 留下完成记录
- [ ] 已通知评审 Agent（如需评审）

---

## 七、项目快速参考

- **项目名**：novel-agent / Project11 — AI 小说创作平台
- **技术栈**：Python 3.11 + FastAPI + LangGraph + LiteLLM / Next.js 14 + TailwindCSS
- **核心架构**：三阶段 LLM 流水线（草稿→精修→评估）+ 实时流式输出 + BYOK 多 provider + RAG 记忆检索
- **数据库**：PostgreSQL 16 + pgvector + Redis（腾讯云）
- **主入口**：后端 `backend/app/main.py`，前端 `frontend/src/app/`
- **更多文档**：`README.md`、`AGENTS.md`、`CHANGES.md`

---

*本文档由主工作树维护，各 Agent 工作树同步更新。修改需经 Claude 协调确认。*
