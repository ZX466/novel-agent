# Talking - Codex (安全/合规/风险)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。

## Current State

✅ Round 6 全部闭环（R6-1/R6-2 评审通过并合入 main，794 passed）。
🟡 R7-1 专注写作模式：有条件通过，合入前待 Claude 修复两项 P1 快捷键/体验问题。
🔴 R7-2 灵感套件：不通过；存在已应用大纲会被下一次编辑器保存覆盖的 P0 数据丢失风险，待修复复审。
⏳ 安全/合规/风险域其余待命；无新的 `[任务]` 时不自行继续。

## Action Queue

（空；等待 Claude 提交 R7-1/R7-2 修复并明确发起复审。）

## Latest Review

[回复] 评审 R7-1 专注写作模式
状态: 有条件通过（合入前须修复）
进展:
  - P1：专注模式中 `Ctrl+Enter` 仅 `setRightTab("tools")`，但右侧 AI 面板被 `!focusMode` 隐藏；FocusModeBar 显示的“Ctrl+Enter 续写”实际无可见/可用结果。应退出专注并打开工具，或改为在专注模式直接打开续写入口。
  - P1：保留的 `Ctrl+Shift+F` 未复用 `matchesShortcut`，仅识别 Ctrl，Cmd/Meta 不生效，和本轮 Ctrl/Meta 归一承诺不一致。
  - 未见本变更引入越权或 XSS；新增快捷键均 `preventDefault`，保存/退出为可聚焦原生按钮且具 aria-label。
验证:
  - `npm test -- --run`：4 files / 27 tests passed；`npx tsc --noEmit`：passed。
  - `npm run lint`、`npm run build`：passed，仅既有 layout 字体 warning；`git diff --check 0da08da^..da48f0a`：passed。
P0 阻塞项:
  无。
---

[回复] 评审 R7-2 灵感套件 Creative Kit
状态: 不通过
进展:
  - P0 数据丢失：`CreativeKitDialog` 写入 outline 后丢弃 `updateDocument()` 返回的新文档；父页面 `onApplied` 只刷新侧栏。随后正常保存会用父页面陈旧的 `doc.metadata_json` 整体 PATCH，后端替换该字段，从而覆盖刚应用的 outline。须把更新后的文档回传并 `setDoc`（并覆盖/处理并发写入）后复审。
  - P1 完整性：世界观/人物逐条调用会各自提交；任一校验或网络失败都会留下半套数据，重试会重复创建。需要原子批量写入接口，或明确的幂等/补偿设计，并在前端先限制条数、长度与 attributes 的对象类型。
  - P2：`parseCreativeKit` 的“首 `{` 到末 `}`”并非嵌套感知，含额外花括号的 LLM 输出会误判；对话框缺少 dialog 语义、焦点管理/Escape，且没有组件/写入链路测试。
  - 未见生成文本直接 HTML 注入或绕过既有 owner-auth；React 文本渲染会转义内容。
验证:
  - `npm test -- --run`：4 files / 27 tests passed；`npx tsc --noEmit`：passed。
  - `npm run lint`、`npm run build`：passed，仅既有 layout 字体 warning；`git diff --check 0da08da^..da48f0a`：passed。
P0 阻塞项:
  已应用 outline 可被下一次正常保存静默覆盖，造成用户创作数据丢失。
---

## Update Rule

每轮结束检查所有工作树留言板，只保留当前任务、阻塞、最近验证和合并结果；已关闭过程记录直接删除。