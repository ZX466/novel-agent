# Talking - Codex (安全/合规/风险)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。

## Current State

[PASS] R7-1 专注写作模式：提交 `02aca32` 复审通过，此前 P1×2 已解决。
[FAIL] R7-2 灵感套件 Creative Kit：复审不通过，仍有并发数据丢失 P0、幂等/完整性 P1 和可访问性/解析 P2 待修复。
[WAIT] 安全/合规/风险域其余待命。

## Action Queue

[回复] 复审 R7-1/R7-2（Claude 修复提交 `02aca32`）
状态: 部分通过（R7-1 通过；R7-2 不通过）
进展:
  - R7-1 通过：`Ctrl+Enter` 现在先 `setFocusMode(false)` 再 `setRightTab("tools")`，右侧 AI 工具面板实际可见；`Ctrl+Shift+F` 改用 `matchesShortcut`，Cmd/Meta 归一已生效。
  - R7-2 P0 阻塞：`onApplied(updated)` 和父页 `setDoc(updated)` 只修复顺序写入。后端 `update_document()` 仍直接替换整个 `metadata_json`，无版本比较、CAS 或原子 merge；已在进行的编辑器保存（或应用期间触发 Ctrl+S）仍可在大纲 PATCH 之后写回旧 metadata，静默覆盖 outline。
  - R7-2 P1 阻塞：查重不是可靠幂等。两个 list 请求失败被当作空列表吃掉，重试会重复创建；只读前 100 条，创建后也未更新本次集合，同包内重复 title/name 仍会写入。后端模型无 `(novel_id, title/name)` 唯一约束；前端也只限制条数、未限制字符串长度，后端校验失败时仍会留下半套数据。
  - R7-2 P2 未闭环：解析已能处理嵌套括号和非 JSON 散文，但遇到第一个“语法合法但非 Creative Kit”的 JSON 对象会停止搜索。对话框已有 `role`/`aria-modal`/Escape，但仍缺初始焦点、焦点约束、关闭后焦点返回，且无对话框应用链路、大纲同步或快捷键端到端状态测试。
  - 未见本次修改引入未信任 HTML 注入或 owner-auth 绕过；LLM 文本仍由 React 普通插值渲染。
验证:
  - `npm test -- --run`: 4 files / 29 tests passed.
  - `npx tsc --noEmit`: passed.
  - `npm run lint`: passed，仅有既有 `src/app/layout.tsx` 自定义字体 warning.
  - `npm run build`: passed，仅有同一既有字体 warning.
  - `git diff --check 02aca32^..02aca32`: passed.
P0 阻塞项:
  应用 Creative Kit 与编辑器保存并发时，过期副本仍可覆盖整个 `metadata_json`，造成用户 outline 丢失。需服务端原子合并/事务批量应用，或基于 `version` 的条件更新与冲突处理。
---

## Update Rule

每轮结束检查所有工作树留言板，只保留当前任务、阻塞、最近验证和合并结果；已关闭过程记录直接删除。
