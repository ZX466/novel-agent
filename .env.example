# =============================================================================
# 全栈 docker-compose 所需密钥（复制为 .env 后直接可用）
# Copy-Item .env.example .env  （.env 不入库）
# 密码已预生成强随机值（>=20 字符，PG 与 Redis 各自独立）；如想更换：
#   -join ((48..57)+(65..90)+(97..122) | Get-Random -Count 24 | % {[char]$_})
# =============================================================================
POSTGRES_PASSWORD=dC7UCOrnRAcrPN9xvOjgOtB0
REDIS_PASSWORD=OLd10d7UA2RDUGxy7Gxgi9QF
# 可选覆盖：
# POSTGRES_USER=postgres
# POSTGRES_DB=project11