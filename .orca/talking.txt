# Talking - kiro (接口/兼容性)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。任务只认 `[任务]` 标记。

## Current State

🟡 **R9-④⑥ 提示词增强+字数控制**：设计过审（`7558a58`）→ Claude 实施（`48bb1d2`）→ codex 交叉复核 3 P1 → **Claude 已修复（`b66c9d0`，main 双远程已推）**，待 codex 复核确认。

**修复明细（`b66c9d0`）**：
- P1-1：`_build_writing_context` 改 async，两处 DB 查询真正 await（原被 except 静默吞掉，前文背景/中位数从不生效）；新增带假 session 回归测试
- P1-2：推导 target 回写 `state["target_word_count"]`，提示词与 post-check 同源
- P1-3：ChatRequest 4 字段 Field 边界（ge/le/title max_length=200），5 越界用例 422
- P2-1：达标时清空 `word_count_retry`
- 回归 486 passed（基线 475）

**保留的实施约束**：
1. regenerate 不得从 refine 重跑 draft（拓扑）——当前实现为"下次迭代强化指令"✅
2. 字数口径 codepoint（len()），前端 countWords 不参与验收 ✅
3. 前端 transport 用 `prepareSendMessagesRequest`，topic 标签 fallback 禁用
4. 人物树接③ serialize_graph（③ 合入后做）
5. 默认 1000 字偏短——随 outline/首章自适应（P2-4 未做）

## Current Queue

- [ ] 等 codex 复核 `b66c9d0` → 通过后 Claude 做 P2-3 前端 transport（端到端闭环）
- [ ] P2-2 人物树接 serialize_graph（依赖③合入 main）
- [ ] P2-4 默认字数自适应（低优）

## Notes

- Pi 评估结论已并入实施；历史提交：设计 `7558a58`、实施 `48bb1d2`、复核 `b0161a6`、修复 `b66c9d0`。

## Update Rule

只保留当前任务、阻塞、最近验证；已关闭任务只留一行结果。
