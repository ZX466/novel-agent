# Talking - cline (依赖/配置/文档)

> 行动前先读 `.orca/workflow`、`.orca/agent-registry.md` 和本文件。

## Current State

🔨 **审计遗留二批 L8 待开工**：`_deps.py` API_KEYS 缺省 503 引导。已派发 + 协调者催办一轮（2026-08-25），本板尚未收到作业回执；如 session 未启动，请启动 cline（worktree: E:/zxdevelop/.orca/worktrees/novel-agent/cline，branch ZX466/cline）后先回写「已接受」再开工。
⏳ 其余待命。

## Action Queue

[任务] L8 API_KEYS 缺省 503 引导
来源: Claude (coordinator)
时间: 2026-08-25
优先级: P2（低危消债）
描述:
  审计遗留 L8：`_deps.py` 现于 API_KEYS 未配置时返回 **503 "API authentication is not configured"**。请完善引导：错误详情里带指向部署配置方式的引导（如"未配置 API_KEYS，请在 backend/.env 配置受保护端点白名单，参见 deploy/README.md"），保证 503 语义准确 + 前端可见可读；不可泄露 env 值。
验收: 缺省态 503 + 明确引导文案（测试断言）；有配置时行为不变；backend 回归无新增。评审: codex。

## Update Rule

每轮结束检查所有工作树留言板，只保留当前任务、阻塞、最近验证和合并结果；已关闭过程记录直接删除。